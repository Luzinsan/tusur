     2.3 Стандартные ввод, вывод и диагностика.
         Перенаправление ввода и вывода

     Когда выполняется команда UNIX, открываются три стандартных файла:

     - стандартный ввод (дескриптор 0);
     - стандартный вывод (дескриптор 1);
     - стандартный вывод диагностических сообщений (дескриптор  2).

     По умолчанию стандартные файлы связаны с терминалом.

     SHELL позволяет переадресовать эти потоки ввода-вывода с  помощью
указаний в командной строке:

     >f1   - стандартный вывод в файл f1 (новый файл);
     >>f1  - добавление вывода в файл f1 (старый файл);
     <f1   - ввод из текстового файла f1;
     n>f1  - переключение потока вывода с дескриптором n в файл f1;
     n>>f1 - то же, но с добавлением в файл f1.

     Например, команда p1 1>>f1 2>null пошлет стандартный вывод  прог-
раммы p1 в конец файла f1, а вывод диагностики - в фиктивный файл (по-
давление вывода).



     2.3 Стандартные ввод, вывод и диагностика.
         Перенаправление ввода и вывода

     Когда выполняется команда UNIX, открываются три стандартных файла:

     - стандартный ввод (дескриптор 0);
     - стандартный вывод (дескриптор 1);
     - стандартный вывод диагностических сообщений (дескриптор  2).

     По умолчанию стандартные файлы связаны с терминалом.

     SHELL позволяет переадресовать эти потоки ввода-вывода с  помощью
указаний в командной строке:

     >f1   - стандартный вывод в файл f1 (новый файл);
     >>f1  - добавление вывода в файл f1 (старый файл);
     <f1   - ввод из текстового файла f1;
     n>f1  - переключение потока вывода с дескриптором n в файл f1;
     n>>f1 - то же, но с добавлением в файл f1.

     Например, команда p1 1>>f1 2>null пошлет стандартный вывод  прог-
раммы p1 в конец файла f1, а вывод диагностики - в фиктивный файл (по-
давление вывода).


 